// Placeholder shell. The mobile-first capture UI (PR-013/014, PR-059..069)
// is built in slice 10 — this stub only proves the workspace builds and
// deploys through web/Dockerfile.
export function App() {
  return (
    <main>
      <h1>BamForm</h1>
      <p>Preventive Maintenance Record and Approval System</p>
    </main>
  );
}
