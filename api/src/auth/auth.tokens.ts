/** DI tokens for values that aren't classes (so they can't use `@Injectable()`/type-based injection). */
export const BLIND_INDEX_KEY = Symbol('BLIND_INDEX_KEY');
export const ACCESS_TOKEN_SERVICE = Symbol('ACCESS_TOKEN_SERVICE');
