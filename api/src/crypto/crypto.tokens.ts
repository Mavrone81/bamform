/** DI tokens for crypto values that aren't classes constructed by Nest itself. */
export const FIELD_ENCRYPTION_SERVICE = Symbol('FIELD_ENCRYPTION_SERVICE');
export const RECORD_SIGNING_SERVICE = Symbol('RECORD_SIGNING_SERVICE');
